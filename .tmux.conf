set -g prefix M-q
set -g mouse on
bind -T root MouseUp2Pane paste # middle-button
set -g base-index 1
setw -g pane-base-index 1
set-option -g history-limit 50000
set -g default-terminal "xterm-256color"
#set-window-option -g xterm-keys on # ctrl-arrows
